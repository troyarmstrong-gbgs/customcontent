{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "bash scripts/fetch-platform-docs.sh; bash scripts/fetch-skills.sh; bash scripts/fetch-broker-queries.sh; uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/healthz",
    "_comment_timeout": "180s window covers cold starts where both fetch scripts hit the network: 5-10s each in fast conditions, up to 50s under load. The previous 30s tripped on the broker-test bootstrap.",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
